this feature disable by default

diff --git a/elf/dl-map-segments.h b/elf/dl-map-segments.h
index 139004ae..da146730 100644
--- a/elf/dl-map-segments.h
+++ b/elf/dl-map-segments.h
@@ -191,6 +191,7 @@ _dl_map_segments (struct link_map *l, int fd,
                               -1, 0);
               if (__glibc_unlikely (mapat == MAP_FAILED))
                 return DL_MAP_SEGMENTS_ERROR_MAP_ZERO_FILL;
+				__madvise((caddr_t) zeropage, zeroend - zeropage, MADV_MERGEABLE);
               if (__is_decorate_maps_enabled ())
                 {
                   if (l->l_name != NULL && *l->l_name != '\0')

